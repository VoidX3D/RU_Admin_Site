# Vite client — only public vars here (no secrets!)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_PRODUCTION_MODE=true

# Server-only (Vercel dashboard) — DO NOT put these in .env file:
# SUPABASE_URL, SUPABASE_SERVICE_KEY, ADMIN_PASSWORD, ADMIN_EMAIL, MASTER_KEY
